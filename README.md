# ISI Project 
# Lidia Muca & Laura J. Illán Ferrández

Base skeleton for the ISI web app.

- Vanilla HTML, CSS, JS (ES modules)
- Simple client-side router
- UI helpers
- `data/db.json` will act as the mock database

We will plug in the actual features once requirements are defined.
